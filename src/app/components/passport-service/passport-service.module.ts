import { NgModule } from '@angular/core';
import { PassportServiceRoutingModule } from './passport-service-routing.module';

@NgModule({
  imports: [PassportServiceRoutingModule],
})
export class PassportServiceModule {}
